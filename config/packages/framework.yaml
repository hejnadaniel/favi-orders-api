# see https://symfony.com/doc/current/reference/configuration/framework.html
framework:
    secret: '%env(APP_SECRET)%'
    # Stateless JSON API: no sessions, no CSRF, no form handling.
    session: false

when@test:
    framework:
        test: true
